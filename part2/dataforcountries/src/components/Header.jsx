const Header = () => {
  return (
    <h1>
      This is a simple application that displays a list of countries per user
      input and the weather in the capital of chosen country.
    </h1>
  );
};

export default Header;
